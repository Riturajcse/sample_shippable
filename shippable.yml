language: node_js

build:
  ci:
    - echo 'hello'
    - docker build -t="riturajcse/sample_shippable:$BRANCH.$BUILD_NUMBER" .
    - docker push riturajcse/sample_shippable:$BRANCH.$BUILD_NUMBER
    - ssh ubuntu@54.200.54.152 ls -a
    
integrations:
  hub:
    - integrationName: dockerhub
      type: docker
 
    
