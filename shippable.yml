language: node_js

build:
  ci:
    - echo 'hello'
    - docker build -t="riturajcse/sample_shippable:latest" .
    - docker push riturajcse/sample_shippable:latest
    
integrations:
  hub:
    - integrationName: dockerhub
      type: docker
 
    
